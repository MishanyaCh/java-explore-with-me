CREATE TABLE IF NOT EXISTS statistics (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    service_name VARCHAR(255),
    uri VARCHAR(255),
    IP_address VARCHAR(15),
    date_of_service_request TIMESTAMP WITHOUT TIME ZONE
);